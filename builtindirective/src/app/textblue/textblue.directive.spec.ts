import { TextblueDirective } from './textblue.directive';

describe('TextblueDirective', () => {
  it('should create an instance', () => {
    const directive = new TextblueDirective();
    expect(directive).toBeTruthy();
  });
});
